# Boilerplate Patch Report — {DATE}

## Verdict: PATCH

Severity score <= 10 AND no architectural triggers. Apply fixes locally.

## Fixes

{For each finding, abbreviated form:}

- **[{rule_id}]** at `{path}:{line}` — {one_line_reason}
  ```diff
  - {old}
  + {new}
  ```

## Roll-up

| Severity | Count |
|---|---|
| minor | {N} |
| advisory | {N} |
| suggestion | {N} |

Effort: < 30 minutes total.

---

*See full audit at reports/history/{DATE}/codebase_analysis.md.*
